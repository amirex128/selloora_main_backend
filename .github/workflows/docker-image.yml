name: ci_selloora_main_backend

on:
  push:
    tags:
      - 'release-*'


jobs:
  # build docker image and push to https://registry.hamyarsale.ir custom registry with this username and password: amirex128 and a6766581
  build-docker:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: build docker image
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: hamyarsale/selloora_main_backend
          tag_with_ref: true
          tag_with_sha: true
          add_git_labels: true
          push: true
          tags: latest
          registry: registry.hamyarsale.ir
      - name: send telegram message on push
        uses: appleboy/telegram-action@master
        with:
          to: 5426495351
          token: 5764924007:AAHSOlyZZWF7NL6_P-jiY2RxLl2-AJsgKGk
          message: |
            ${{ github.actor }} created commit:
            Commit message: ${{ github.event.commits[0].message }}
            
            Repository: ${{ github.repository }}
            
            See changes: https://github.com/${{ github.repository }}/commit/${{github.sha}}
